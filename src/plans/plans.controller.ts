import { Body, Controller, Get, Param, Post, Query } from '@nestjs/common'; import { IsEnum, IsNumber, IsOptional, IsString, Min } from 'class-validator'; import { PrismaService } from '../common/prisma.service'; import { BillingInterval } from '@prisma/client';
class PlanDto { @IsString() name!: string; @IsString() slug!: string; @IsOptional() @IsString() description?: string; @IsNumber() @Min(0) price!: number; @IsOptional() @IsString() currency?: string; @IsEnum(BillingInterval) interval!: BillingInterval; @IsOptional() @IsNumber() @Min(0) trialDays?: number; }
@Controller('plans') export class PlansController { constructor(private prisma:PrismaService){} @Get() list(@Query('active') active='true'){ return this.prisma.plan.findMany({where: active==='true'?{isActive:true}:undefined,orderBy:{price:'asc'}});} @Get(':slug') get(@Param('slug') slug:string){return this.prisma.plan.findUnique({where:{slug}});} @Post() create(@Body() dto:PlanDto){return this.prisma.plan.create({data:{...dto,price:dto.price,currency:dto.currency||'USD',trialDays:dto.trialDays||0}});} }
